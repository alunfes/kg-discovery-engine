[project]
name = "kg-discovery-engine"
version = "0.1.0"
description = "KG Discovery Engine — hypothesis generation via KG operators"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
